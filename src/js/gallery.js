const Gallery = function(opts) {
  this.container = opts.container;
  // this.items = Array.from(container.querySelectorAll('li'));
  // this.container.addEventListener('mouseenter', toggleImage)
  // this.container.addEventListener('mouseleave', toggleImage)
}

function toggleImage(e) {
  const el = e.target;
}

module.exports = Gallery;
